import { createStyles } from "@mantine/styles";

export const useStyles = createStyles({
  container: {
    position: "fixed",
    bottom: "40px",
    right: "40px",
    display: "flex",
    flexDirection: "column",
    gap: "4px",
    justifyContent: "end",
    maxWidth: "300px",
  },
});
